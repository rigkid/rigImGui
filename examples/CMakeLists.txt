# Examples for rigImGui (standalone CMake projects - configure each folder)

# Example: host_shell
# Also: sample_menubar, example_filebrowser, example_ImGuizmo
#
#   cmake -S <example> -B <example>/build
#   cmake --build <example>/build
